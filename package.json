{
  "name": "studybuddy",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "DATABASE_URL=file:./dev.db jest --runInBand",
    "typecheck": "tsc --noEmit",
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate --schema prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.2"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "prisma": "^6.19.2",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  }
}
